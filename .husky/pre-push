# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# echo "🧪 Running Unit Tests before pushing..."
# flutter test

#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# เรียก script ที่เราสร้างไว้
./scripts/sonar_gate.sh