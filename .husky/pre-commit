#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🐶 Husky: Manual checking on STAGED files..."

# 1. หาไฟล์ .dart ที่ถูก Staged (เฉพาะที่ Add มา)
# - diff --cached --name-only : ดูชื่อไฟล์ใน staging area
# - diff --diff-filter=ACMR : เอาเฉพาะไฟล์ที่ Add, Copy, Modify, Rename (ไม่เอา Deleted)
# - grep .dart$ : เอาเฉพาะไฟล์ลงท้ายด้วย .dart
# staged_dart_files=$(git diff --cached --name-only --diff-filter=ACMR | grep ".dart$" || true)

# # 2. เช็คว่ามีไฟล์ Dart ไหม? (ถ้าไม่มีก็ข้ามการ format/analyze)
# if [ -z "$staged_dart_files" ]; then
#   echo "✨ No Dart files changed. Skipping format and analyze."
#   echo "   But will still run tests and SonarQube..."
  
#   # ข้ามไปรัน tests และ SonarQube
#   skip_dart_checks=true
# else
#   skip_dart_checks=false
#   # แสดงรายชื่อไฟล์ที่จะเช็ค
#   echo "📝 Checking these files:"
#   echo "$staged_dart_files"
# fi

# # ---------------------------------------------------------
# # 3. รัน Dart Format (เฉพาะไฟล์ที่เลือก)
# # ---------------------------------------------------------
# if [ "$skip_dart_checks" = false ]; then
#   echo ""
#   echo "✨ Running Dart Format..."
#   dart format $staged_dart_files
# fi

# # ---------------------------------------------------------
# # 4. รัน Flutter Analyze (เฉพาะไฟล์ที่เลือก)
# # ---------------------------------------------------------
# echo ""
# echo "🔍 Running Flutter Analyze..."
# echo "⏭️  Skipping Flutter Analyze (testing SonarQube)"

# # ---------------------------------------------------------
# # 5. รัน Tests พร้อม Generate Coverage
# # ---------------------------------------------------------
# echo ""
# echo "🧪 Running Flutter Tests with Coverage..."
# flutter test --coverage

# if [ $? -ne 0 ]; then
#   echo ""
#   echo "❌ Tests Failed! Please fix test errors."
#   exit 1
# fi

# echo "✅ Tests passed! Coverage generated at coverage/lcov.info"

# # ---------------------------------------------------------
# # 6. รัน SonarQube Analysis และเช็ค Quality Gate
# # ---------------------------------------------------------
# echo ""
# echo "🔍 Running SonarQube Analysis..."

# # เรียกใช้ script ที่เตรียมไว้แล้ว
# bash scripts/sonar_gate.sh

# # เก็บ exit code
# SONAR_EXIT_CODE=$?

# # แสดง Coverage Report (เสมอ!)
# echo ""
# echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
# echo "📊 Coverage Summary:"
# if [ -f "coverage/lcov.info" ]; then
#   total_lines=$(grep -c "^DA:" coverage/lcov.info || echo "0")
  
#   if [ "$total_lines" -gt 0 ]; then
#     coverage=$(awk "BEGIN {printf \"%.1f\", ($covered_lines/$total_lines)*100}")
#     echo "   Coverage: ${coverage}%"
#     echo "   Lines: ${covered_lines}/${total_lines}"
#   else
#     echo "   No coverage data available"
#   fi
# else
#   echo "   Coverage file not found"
# fi
# echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# # เช็ค Exit Code
# if [ $SONAR_EXIT_CODE -ne 0 ]; then
#   echo ""
#   echo "❌ SonarQube Quality Gate Failed!"
#   echo "💡 Please check:"
#   echo "   - Maintainability Rating must be A"
#   echo "   - No Security Issues"
#   echo "   - No Reliability Issues"
#   echo "   - Coverage must meet threshold"
#   exit 1
# fi

# echo ""
# echo "✅ All checks passed (including SonarQube Quality Gate)!"
# exit 0